notifications:
  email: false

language: cpp
sudo: false

branches:
  only:
  - master

compiler:
  - clang
os:
  - linux

addons:
  apt:
    sources:
    - ubuntu-toolchain-r-test
    - llvm-toolchain-trusty-5.0		
    packages:
    - gcc-6
    - g++-6
    - clang-5.0
    - clang-tidy-5.0
    - llvm-5.0-dev
    - texinfo
    - zlib1g-dev
    - bear

install:
  - chmod +x $TRAVIS_BUILD_DIR/clean-commands.py

script:
  - cd $TRAVIS_BUILD_DIR/binutils-gdb
  - ./configure --target=mipsel-unknown-linux-gnu --prefix=$TRAVIS_BUILD_DIR/binutils-gdb/cross/mips --with-system-zlib
  - bear make > /dev/null
  - $TRAVIS_BUILD_DIR/clean-commands.py
  - run-clang-tidy-5.0.py -clang-tidy-binary clang-tidy-5.0 -header-filter='.*' -checks='*,-llvm-header-guard,-readability-named-parameter,-google-readability-braces-around-statements,-llvm-include-order,-google-readability-braces-around-statements'
  - make install > /dev/null
